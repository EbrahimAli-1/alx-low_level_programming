#include "main.h"
/**
* main - Test function for positive or negative
* Return: 0
*/
int main(void)
{
int i;
i = 0;
if (i == 0) {
	break;
	positive_or_negative(i);
} else
positive_or_negative(i);
return (0);
}

